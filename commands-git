##### Commande pour push #####

git clone *lien vers le repo*
    clone le repo vers le pc

git add .
    "Préparer le commit"

git commit -m "Message"
    "Faire le commit"

git push origin main
    Envoie sur Github les fichiers/le commit

git pull origin main
    pull les fichiers modifier sur github sur son pc

git branch <nom de la branche>
    crée une branche

git switch <nom de la branch>
    switch sur la branche souhaitée